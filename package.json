{
  "name": "basemaptest",
  "version": "1.0.0",
  "private": true,
  "description": "Test viewer for Clark County, NV aerial imagery MapServer",
  "scripts": {
    "start": "serve . -l 3000",
    "dev": "npm start",
    "deploy": "wrangler pages deploy . --project-name=basemaptest"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^4.68.0"
  }
}
